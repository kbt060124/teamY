import SearchBar from "@/Components/SearchBar";
import React from "react";

const Search = (props) => {
    console.log(props);
    return (
        <div>
            <SearchBar />
            
        </div>
    );
};

export default Search;
